import 'package:flutter/material.dart';

class marketpalceScreen extends StatelessWidget {
  const marketpalceScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(child: Text('MARKETPLACE',style: TextStyle(fontSize: 30),));
  }
}
